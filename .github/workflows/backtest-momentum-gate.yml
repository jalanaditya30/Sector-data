name: backtest-momentum-gate
on:
  workflow_dispatch:
  push:
    paths:
      - early/momentum_test.py
      - .github/workflows/backtest-momentum-gate.yml
permissions:
  contents: write
concurrency:
  group: backtest-momentum-gate
  cancel-in-progress: true
jobs:
  research:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Momentum gate temporarily blocked for pre-result corporate-action plumbing fix. No result generated."
