name: refresh-v62-live-retired
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "V6.2 R2/R5 live candidate generation was retired on 2026-09-08."
          echo "This workflow is intentionally manual-only and does not generate or commit signals."
